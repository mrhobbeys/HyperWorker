# PROJECT: Onboarding — <Client Name>

> Client-onboarding project. Bootstrapped from `schemas/projects/client-onboarding/`.

## Status

active

## Objective

Onboard <Client Name> onto the platform within the contractual SLA, with the integrations declared in `OR-001.integrations_in_scope` configured and validated.

## Scope

### Included
- Account provisioning + SSO
- Historical data migration (if `data_volume` > 0)
- Integration configuration: <list>
- Training session
- Handoff package + runbook

### Explicitly Excluded
- Anything not in the signed contract scope

## Operating Reality

`[OR-001#<short-hash>]` declares: contract SLA, compliance scope, client designated contact.

## Schema

Bootstrapped from `schemas/projects/client-onboarding/` with harness version `5.0`.

## Completion Criteria

- [ ] All onboarding tasks complete; client signs off on handoff.
- [ ] No stale citations.
- [ ] Findings classified `client_specific: true` filed under client-scope tag; findings classified `client_specific: false` proposed for cross-project promotion.
- [ ] Anti-patterns captured for any vendor quirk encountered (helps next client).

## Started

<YYYY-MM-DD>

## Archived

(blank until done)
