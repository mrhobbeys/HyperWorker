# site-review-repair schema

Per-site Review & Repair workstream for the Website-Repair program. One site per project, isolated workspace, own `.hyperworker`. Sibling workstreams (Monetization, SEO) and the Recovery program are separate. Flow: scope/access → crawl baseline → render audit → broken links/redirects → triage → fix plan (operator-approved) → implement → verify → handoff.
