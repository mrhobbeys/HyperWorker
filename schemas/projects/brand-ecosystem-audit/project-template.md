# PROJECT.md — {{ project_name }}

## Brand
{{ brand_identity }}

## Owned hub (should own the brand's name)
{{ owned_hub_url }}

## Surfaces to audit (the units)
{{ surfaces }}

## Primary goal
{{ primary_goal }}

## Audience
{{ audience }}

## Dispatch mode
{{ dispatch_mode }}  *(single-agent | separate-chats; separate-chats always for social/login surfaces)*

## Deliverable
{{ deliverable_path }}

## Phase shape
- **A — Frame.** Brand dossier + OR + surface inventory (T-000).
- **B — Per-property fan-out.** Deep-dive (T-001) + neutral-account ranking check (T-002), one per property.
- **C — Cross-brand.** Link/handle integrity (T-003) + competitor benchmark (T-004).
- **D — Synthesize.** Build manifest (T-005) -> master synthesis to a paths menu (T-006).

## Hard scope boundaries
- One property per audit unit; no cross-contamination.
- Broken/works claims verified live; ranking checks neutral-account with method recorded.
- Login-required data not accessible = OPEN ITEM, never guessed.
- Synthesis ships a MENU (>=3 paths + foundational fixes), never one forced plan.

## Completion criteria
- Every surface audited or accounted (dead/missing/duplicate).
- All rankings on one common scale, each with neutral-account method.
- Manifest indexes everything; master report = paths menu + decision aid + foundational fixes.
- >=1 disconfirming finding; T-006 council converged; operator review done.
