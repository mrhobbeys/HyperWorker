# 00-REFERENCE-rules.md — {{ project_name }}

Lower tier ordinal wins on conflict.

## Tier 1: EVIDENCE-INTEGRITY (NON-NEGOTIABLE)
Render before you rule. Confirm broken/works in a real browser (`verified_live: true`).
Ranking checks run from a NEUTRAL account (no personalization) and record the method.
Provenance-tag everything; no recommendation on ASSUMED alone. Login-missing data = open item.
@@SCAN_1_1  @@SCAN_1_2

## Tier 2: BRAND-TRUTH (SCOPE)
Serve OR.primary_goal and the real identity. The owned hub should own the name; name leaks
where authority lands elsewhere. Don't invent positioning; surface options.
@@SCAN_2_1

## Tier 3: AUDIT-QUALITY (TECHNICAL)
Every surface audited or accounted (dead/missing/duplicate). One property per unit. All
rankings on ONE common scale. >=1 disconfirming finding.
@@SCAN_3_1  @@SCAN_3_2

## Tier 4: STYLE
Citations: [PROP/PA/RC-NNN#hash], [F/DEC/MAN/PATH-NNN#hash]. Lead the master report with the
bottom line + evidence inventory, then the paths menu.

## Synthesis output template (the master report — same as market-gap T-008 v1.1)
Bottom line -> Evidence Inventory (+ open items) -> common-scale scorecard -> cross-board
synthesis -> consolidated visibility picture -> identity/positioning backbone -> STRATEGIC
PATHS MENU (3-6, each effort/payoff/time/levers/risks/top-moves) -> decision aid (table +
recommended default + sequence, not forced) -> foundational fixes regardless of path ->
unified funnel + consistency checklist + per-path metrics.

## Operator-attention rule (SOUL: boil the ocean for agents, not for humans)
Agent reruns are cheap — run/rerun/test freely. The operator's attention is the bottleneck —
batch decisions, ask <30-second pick-one questions, pull the human in only for the irreducible.
