# single-opportunity schema
Dedicated project for ONE named deal so it never gets lost. On-demand: the
orchestrator spins this up when the operator says "stay on top of this one."
Tasks: qualify -> requirements/stakeholders -> solution & pricing -> proposal ->
submit & follow-up -> close & retro. Operator approves before any send/submit.
