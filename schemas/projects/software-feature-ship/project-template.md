# PROJECT: <Feature Name>

> Software-feature-ship project. Bootstrapped from `schemas/projects/software-feature-ship/`.

## Status

active

## Objective

<One sentence: what this feature lets the user do that they cannot today, and on which surface it ships.>

## Scope

### Included
- <surface — e.g., `users` table migration>
- <surface — e.g., `/api/auth` POST endpoint>
- <surface — e.g., `<LoginButton>` component on `/login` page>

### Explicitly Excluded
- <e.g., mobile app login (separate project)>
- <e.g., legacy session migration (separate project)>

## Operating Reality

Anchored to `[OR-001#<short-hash>]`. CI/CD provider and required status checks declared there.

## Schema

Bootstrapped from `schemas/projects/software-feature-ship/` with harness version `5.0`.

## Completion Criteria

- [ ] All tasks complete; CI green on the target branch.
- [ ] Production deploy task complete; smoke test passes.
- [ ] Rollback plan present for every elevated/critical change.
- [ ] No stale citations across project artifacts.
- [ ] Findings written for any cross-surface coupling discovered (helps next ship).

## Started

<YYYY-MM-DD>

## Archived

(blank until done)
