# site-seo schema
Per-site SEO recovery as an ordered sequence of deep-focus phases. Report-first; each phase ends with a
server-side handoff when server/dev work is needed; ranking + duplicate-content detection uses Haiku
subagents (ranking-test-haiku.md); DMCA is a late phase; the final phase is open-ended (ongoing SEO plan).
