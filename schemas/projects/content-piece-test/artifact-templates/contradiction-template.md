---
deprecated: true
inherited_from: report-synthesis
schema: content-piece-test
---

# Inherited from report-synthesis — NOT active in content-piece-test

This artifact template was copied during the inheritance scaffold. The content-piece schema does not use `claim` or `contradiction` artifact kinds. See `artifact-extensions.yaml` for the active kinds (`source`, `verbatim_keeper`).
